# Kuriftu Admin (UI prototype)

This app is a **UI prototype only**: every page renders hardcoded mock data,
there is no authentication, and nothing talks to the backend. It is not
deployed. Wiring it up requires admin roles, admin auth, and admin CRUD
endpoints in `/backend` — a separate project.
